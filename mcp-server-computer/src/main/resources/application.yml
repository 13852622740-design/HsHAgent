spring:
  appliacation:
    name: mcp-server-computer

  ai:
    mcp:
      server:
        name: ${spring.appliacation.name}
        version: 1.0.0

  main:
    banner-mode: off
    web-application-type: none

logging:
  pattern:
    console:
  file:
    name: ${spring.ai.mcp.server.name}.log

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true